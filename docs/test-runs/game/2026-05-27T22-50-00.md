# Test run — 2026-05-27T22:50:00

## Results

```
TENSIES TEST RESULTS
====================
 PASS  01  Self-update check
 PASS  02  Server health
 PASS  03  Landing screen
 PASS  04  Create game + lobby
 PASS  05  Deep-link join (?join=CODE)
 PASS  06  Invalid code rejection
 PASS  07  Multiplayer lobby sync
 PASS  08  Join-after-start rejection
 PASS  09  Game start + initial render
 PASS  10  Roll + reveal (single player)
 PASS  11  Same-value re-roll (no hang)
 PASS  12  Rate limit + recovery
 PASS  13  Multiplayer broadcast timing
 PASS  14  Roll to win + winner overlay
 PASS  15  Round transition + target cycle
 PASS  16  Target cycling math
 PASS  17  Player disconnect mid-game
 PASS  18  Player reconnect flow
 PASS  19  Host disconnect / host transfer
 PASS  20  Animation integrity (no tearing)
 PASS  21  Console clean (no JS errors)
====================
21/21 passed
```

## Findings

None.

## Notes

- **First run with reconnect feature** — all 21 tests passed including the two new reconnect/disconnect tests (18, 19).

- **Broadcast timing: 1668ms** — back up from last run's 1029ms low. Three most recent: 1029ms, 1668ms, (and 1491ms/1589ms from earlier). All within 1000–2000ms pass window. The 1029ms outlier appears to have been a one-off.

- **Disconnect overlay captured via MutationObserver** — "Waiting for Beta to reconnect…" appeared and was dismissed in ~20ms because `ws.close()` + auto-reconnect fires almost instantly. The dimmed-card state was too transient to capture in a screenshot, but the overlay text was correct.

- **Host disconnect distinctly confirmed** — host's card showed `player-mini disconnected` class; overlay showed "Waiting for Alpha to reconnect…"; `currentHost` still pointed to Alpha's ID (grace period not yet expired); Beta could still roll. This is the expected immediate post-disconnect state.

- **Session expired path observed twice**: (1) Tab 3 opened with Beta's stored credentials while Beta was still live — server rejected reconnect because player wasn't disconnected, "Your session expired" shown correctly; (2) post-browser-close reload — all tabs died simultaneously, new tab tried to reconnect with Beta's credentials, race condition caused server to reject, "Your session expired" shown again.

- **localStorage collision (test artifact only)** — all browser tabs share the same `localStorage` origin, so Tab 3's join attempts (Gamma, Latebird) overwrote the real Beta player UUID with a temporary UUID. This required an explicit `localStorage.setItem(myId, gameCode)` on Beta's tab before each test requiring reconnect. On real mobile use, each player has their own device/localStorage; this collision cannot occur in production.

- **`browser_close` closes all tabs** — closing Tab 2 via the Playwright MCP `browser_close` tool closed the entire browser session, not just that tab. This is a test tooling limitation. Future disconnect tests should use `ws.close()` via `evaluate` to simulate disconnect without killing the tab.

- **Skill updated this run**: Step 0 now checks both `git log` and `git diff`/`git diff --cached` for unstaged changes; Steps 15/15b/16 updated to reflect reconnect feature behavior; summary table expanded to 21 rows.

## Watch next run

- **Broadcast timing** — measurements now: 1589ms, 1491ms, 1029ms, 1668ms. The 1029ms remains the only sub-1100ms reading. If it appears again, investigate delayed-broadcast regression.
- **localStorage collision** — the test explicitly fixes Beta's localStorage before each reconnect step. If Tab 3 operations are added or moved, re-verify that this fix is applied at the right point.
- **Disconnect card dimming** — only confirmed via host-disconnect test (where reconnect wasn't instantaneous). The Beta-disconnect test reconnects too fast to observe the dimmed state in a screenshot; MutationObserver remains the only reliable capture path.
- **`browser_close` behavior** — avoid using it to simulate single-tab close. Use `ws.close()` via `evaluate` instead.
- **30s host transfer** — not tested (would require a 30s wait). Consider adding a dedicated test if host-transfer correctness becomes a concern.
