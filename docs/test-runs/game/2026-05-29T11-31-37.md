# Test run — 2026-05-29 (first run of the audited 19-step skill)

## Results

```
TENSIES TEST RESULTS
====================
Preflight: self-update none (no game-code changes since skill), prior-run review ran (not scored)
 PASS  01  Server health (HEALTH OK, games_active=0 baseline)
 PASS  02  Landing screen (placeholder "Wobbly Porcupine", #landing-error empty, 0 console)
 PASS  03  Create game + lobby (code REYPN, Alpha + HOST, "start solo" msg)
 PASS  04  Deep-link join + lobby sync (code prefilled, URL cleaned, both tabs synced)
 PASS  05  Invalid game code rejection (ZZZZZ → "Game not found" in #join-error)
 PASS  06  Game start + initial render (Round 1, target die, 0/10, both tabs on #game)
 PASS  07  Join-after-start rejection ("Game already in progress")
 PASS  08  Roll + reveal (rc 0→1, btn disabled→re-enabled 1408ms, dice clean)
 PASS  09  Same-value re-roll (5 rolls 1192–1686ms, all advanced, no hang)
 PASS  10  Rate limit + recovery (raw-WS frames; server RATE LIMIT log; btn not stuck)
 PASS  11  Multiplayer broadcast timing (1167ms — within 1000–2000ms band)
 PASS  12  Roll to win + winner overlay ("Alpha", "Next up: roll for 5s") [open-attr capture]
 PASS  13  Sticky winner overlay regression (30 fires, openWindow 3005ms, stillOpen false)
 PASS  14  Round transition + target cycle (R1→R2→R3, target 6→5→4, clean reset)
 PASS  15  Player disconnect mid-game (Tab1 loading "Waiting for Beta…", disconnected:true)
 PASS  16  Player reconnect flow (real reload; identity preserved; both back in game)
 PASS  17  Host disconnect + reconnect (Beta loading, host unchanged; host reload restored)
 PASS  18  Animation integrity (no tearing/clip; flags clear at rest)
 PASS  19  Console clean (0 errors/warnings on both instances)
====================
19/19 passed
```

Run on the audited skill (2 isolated Playwright instances). 60s DISCONNECT_GRACE / RECONNECT_WINDOW_MS in effect.

## Findings

- **Prior-run headline FAIL (Step 19 player reconnect) is CLEARED.** The 2026-05-28 run failed reconnect with a mystery pid and tensies_pid=null/code-present — the classic shared-localStorage signature from running multiple players as tabs in one browser. With separate Playwright instances (isolated profiles) the reconnect succeeds cleanly via a real page reload, identity preserved (f92efadc…). Confirms the diagnosis from 2026-05-29.
- **🔴 winner-overlay capture fix validated.** Step 12 captured winnerName/winnerSub via the dialog `open` attribute. The previous skill watched a nonexistent `visible` class, which would have captured nothing. New approach works.

## Notes

- **Skill discrepancy — Step 10 (rate limit):** the documented "click #roll-btn twice" cannot trigger the server rate limit, because roll() has an `if (state.rolling) return` guard that drops the second click before any frame is sent. Had to send two raw `roll` frames via _state.ws to exercise MIN_ROLL_INTERVAL (server logged RATE LIMIT). Worth updating the step text to prescribe the raw-WS method (the prior log hit the same wall).
- **Throwaway-tab localStorage clobber is real and observed.** The Step-5 invalid-code throwaway tab (same instance-#2 profile) ran its own page-load reconnect against Beta's stored session, the server rejected it (Beta still connected), expireSession cleared instance #2's localStorage. Step 16 therefore required re-seeding tensies_pid/code (captured at join) before the reconnect reload — exactly as the skill anticipates. Worked as designed.
- **Transient "Connection failed" on a stale-session navigate (Step 2):** navigating an instance whose localStorage still points at a now-dead game shows a brief "Connection failed" on landing (failed auto-reconnect, which then clears storage). Benign; a second clean navigate clears it. Start each run from cleared localStorage.
- **Broadcast timing 1167ms** — comfortably mid-band. Series remains healthy.
- **pendingRollTimeouts = 8 at rest** — the known benign stale-ID accumulation (wiped at the next roll's start); core flags (rolling/awaitingAck/pendingRollState) all clear.
- **Target cycle verified live across 3 rounds:** 6→5→4. has_rolled clean ([false,false]) in the fresh round after the spacebar-hammer step — the prior log's orphan-setTimeout contamination did NOT recur (phase state-machine held).

## Watch next run

- **Step 10 rate-limit method** should be fixed in the skill (raw-WS, not double-click). Until then, expect to use the raw-WS bypass.
- **Re-seed discipline for reconnect:** capture each player's pid + GAME_CODE at join; throwaway edge-case tabs will clear the shared profile's localStorage. Step 16/the reconnect must re-seed.
- **Begin each run by clearing localStorage on both instances** (persistent profiles retain stale sessions from prior runs → transient "Connection failed").
- Host-transfer (after the 60s drop) is still not verified — optional slow step if desired.
