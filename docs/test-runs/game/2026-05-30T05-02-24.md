# Test run — 2026-05-30T05:02:24  (full sweep, everything)

Ran every test group in sequence. **Telemetry excluded** per standing request. No product
code changed — verification re-run.

## Results — 44/44 across 5 groups, all green

**SERVER-LOGIC UNIT TESTS** — 8/8 PASS

| # | Result | Description |
|---|--------|-------------|
| 01 | ✅ PASS | host-abandon -> transfer to connected guest; new host resumes |
| 02 | ✅ PASS | round-win during pause deferred; advances on resume |
| 05 | ✅ PASS | disconnect-then-pause rescues the player (no drop) |
| 06 | ✅ PASS | resume reschedules drop for a still-offline player |
| 08 | ✅ PASS | non-host pause frame rejected |
| 09 | ✅ PASS | pause before game start ignored |
| 10 | ✅ PASS | pause for unknown game: no crash |
| 11 | ✅ PASS | join attempt on a paused (started) game rejected |

**22-STEP REGRESSION** — 22/22 PASS

(create/join/start, roll+reveal, same-value re-roll, rate limit, broadcast timing 1659ms, roll-to-win + winner overlay, sticky-overlay spacebar, round transition, pause toggle/status/resume, disconnect/reconnect, host disconnect/reconnect, animation integrity, console clean)

**BROWSER EDGE CASES** — 7/7 PASS

| # | Result | Description |
|---|--------|-------------|
| 03 | ✅ PASS | pause during a non-host's in-flight roll -> wait screen, clean, resume OK |
| 12 | ✅ PASS | three-player pause -> wait screens; 3/3 -> 2/3 -> 3/3; resume |
| 14 | ✅ PASS | concurrent games isolated |
| 15 | ✅ PASS | game gone -> graceful landing |
| 19 | ✅ PASS | XSS-safe player name |
| 20 | ✅ PASS | duplicate names accepted + distinct |
| 21 | ✅ PASS | edge names (blank -> placeholder; truncation to 20) |

**PAUSE CAP FIRES** (PAUSE_MAX=8s) — PASS

countdown 0:08 -> pause_timeout -> fatal frame -> both clients land ("Game ended — it was paused too long.", sessions cleared)

**REAL SERVER RESTART DURING PAUSE** — PASS

kill -9 + relaunch (state wiped) -> both paused clients reconnect, get "Game not found", land ("Connection failed", sessions cleared)

## Findings

None. Full green.

## Notes

- Sequencing matters: the cap test runs on a short-`PAUSE_MAX` server (8 s) and the restart
  test on a normal server; both are swapped in/out around the browser/regression suites,
  which need the normal server.
- Same sandbox friction as prior runs (kill+launch compounds intermittently exit 1/144;
  local Postgres drops) — handled by killing, confirming the port is free, then launching
  the server in a standalone command.

## Watch next run
- Always restore a normal-`PAUSE_MAX` server after the cap test before anything else.
- Telemetry remains untested by request.
