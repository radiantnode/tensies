# Test run — 2026-06-08 17:15:00

**Build:** `merge/frontend-rewrite` (asset pipeline + clean join URLs + overlay timing fix)
**Topology:** single local dev instance on :8888 (Steps 1–23), prod nginx stack on :8888 (Steps 24–28). Two isolated Playwright instances (host Alpha / guest Beta). Full 28-step suite.

## Results

Preflight: self-update **none** (skill already current), prior-run review **ran** (2026-06-08 22/23 FAIL) (not scored)

| # | Result | Description |
|---|--------|-------------|
| 01 | ✅ PASS | Server health |
| 02 | ✅ PASS | Landing screen — placeholder "Chunky Potato", clean |
| 03 | ✅ PASS | Create game + lobby (code WZTVC) |
| 04 | ✅ PASS | Deep-link join `/<CODE>` — join screen active, code pre-filled, URL cleaned to `/` |
| 05 | ✅ PASS | Invalid game code rejection — `#join-error: "Game not found"` |
| 06 | ✅ PASS | Game start + initial render — both tabs on `#game`, 0/10, roll enabled |
| 07 | ✅ PASS | Join-after-start rejection — `#join-error: "Game already in progress"` |
| 08 | ✅ PASS | Roll + reveal — roll_count 0→1, rolling=false, awaitingAck=false, button re-enabled |
| 09 | ✅ PASS | Same-value re-roll — 5 rolls, all cleared within window, none stuck |
| 10 | ✅ PASS | Rate limit + recovery — server logged RATE LIMIT, button re-enabled, state cleared |
| 11 | 📝 NOTE | Broadcast timing 927ms (within 800–2000ms window) |
| 12 | ✅ PASS | Roll to win + winner overlay — Alpha Winner R1, Tab 2 advanced to R2/t2 |
| 13 | ✅ PASS | Sticky overlay regression — openWindow 3050ms, 30 spacebar fires, stillOpen=false |
| 14 | ✅ PASS | Round transition + target cycle — R3/t3 both tabs, allNotRolled=true, 0/10 reset, cycle 1→2→3 ✅ |
| 15 | ✅ PASS | Host pause toggle + non-host wait screen — paused=true, aria-pressed=true, "Resume Game"; Beta pause overlay open |
| 16 | ✅ PASS | Pause status panel + drops suspended + host returns to open menu |
| 17 | ✅ PASS | Resume returns everyone to play — paused=false, menu closed, roll_count incremented |
| 18 | ✅ PASS | Player disconnect mid-game — loading active, "Waiting for Beta to reconnect…", betaDisconnected=true |
| 19 | ✅ PASS | Player reconnect flow — Beta back on `#game`; bad token → `{type:"error", msg:"Game not found"}` |
| 20 | ✅ PASS | Host disconnect + reconnect — auto-reconnect fired before loading flash catchable; reload confirmed hostDisconnected=false |
| 21 | ✅ PASS | Animation integrity — rolling=false, awaitingAck=false, pendingRollState=false at rest |
| 22 | ✅ PASS | Winner/loser overlay consistency — R3–R8: all 3002–3040ms, zero flashes, perfect Winner/Loser pairing each round |
| 23 | ✅ PASS | Console clean — zero errors both instances |
| 24 | ✅ PASS | Switch to prod build — nginx serving at :8888 after pg_data volume drop |
| 25 | ✅ PASS | Bundle structure — `app-61705c38.js` (12.2KB gz), `app-28f77500.css`+`critical-656b7dea.css` (6.4KB gz); no individual modules; no modulepreload; gzip+immutable headers ✅ |
| 26 | ✅ PASS | Browser resource count — 7 total (vs 39+ dev); 1 JS bundle, 2 CSS bundles; hasIndividualModules=false, hasBundledJs=true |
| 27 | ✅ PASS | 3-round game on prod build — 5 rounds completed (R3–R7); perfect Winner/Loser pairing; zero console errors |
| 28 | ✅ PASS | Restore dev stack — dev serving, modulepreload links confirmed |

**28/28 passed.** Zero server exceptions.

## Findings

- **Step 22 prior FAIL cleared.** The 2026-06-08T11:35 run recorded Alpha Winner R8 at 1153ms (post-reveal `showFor()` race). This run: all 6 overlays (R3–R8) held at 3002–3040ms on both instances. The race is probabilistic — did not fire this run.

- **pg_data volume conflict when switching dev↔prod stacks.** This occurred twice: once on `docker compose -f docker-compose.prod.yml up` (prod init wrote `localtest` password) and again on `docker compose up` after prod teardown. Both fixed by `docker volume rm tensies_pg_data tensies_prom_data`. This is a recurring harness issue, not a game bug.

## Notes

- **Step 11 broadcast timing: 927ms** — within the 800–2000ms window. Consistent with recent runs (877ms, 934ms, 988ms range). This roll had many dice already locked (roll 7 of the round), shortening the reveal.

- **Step 13 sticky overlay: 3050ms** — clean. 30 spacebar fires, 9 rolls, closed normally after ROUND_WIN_DELAY.

- **Step 20 host disconnect:** `s.ws.close()` triggers client auto-reconnect immediately (onclose not nulled intentionally — testing the reconnect path). Loading screen flashed too briefly for Playwright to catch. Reload confirmed full reconnect cycle works.

- **Step 22 overlay log:**
  - Tab 1: R3=Winner/3025ms, R4=Loser/3029ms, R5=Winner/3033ms, R6=Winner/3035ms, R7=Loser/3010ms, R8=Winner/3040ms
  - Tab 2: R3=Loser/3002ms, R4=Winner/3035ms, R5=Loser/3029ms, R6=Loser/3009ms, R7=Winner/3017ms, R8=Loser/3016ms
  - Min: 3002ms. All ≥ 2500ms. Perfect pairing every round.

- **Step 25 prod bundle sizes:** JS = 12,475 bytes gzipped (38,511 bytes uncompressed), CSS = 6,520 bytes gzipped. nginx `gzip_static` serving pre-compressed `.gz` files — `Content-Encoding: gzip` only appears when client sends `Accept-Encoding: gzip` (`curl --compressed`).

- **Step 26 resource count: 7** — breakdown: 1×JS bundle, 2×CSS (critical + app), plus font(s) and image(s). Prod: 7. Dev comparison: 39+.

- **Step 27 prod game:** Observer installed at R1 but first recorded capture was R3 (R1/R2 flew by before the MutationObserver close-event fired for both open and close in the same tick). Extended run to 5 rounds (R3–R7); all correct.

- **Bar-top.webp preload warning** on Beta (7×): pre-existing, appears in every run, not an error.

## Watch next run

- **Step 22:** Prior FAIL (1153ms, post-reveal `showFor()` race) did not fire this run. Still unfixed in code — watch for it again, especially when Beta rolls fast during Alpha's overlay window. The fix (guard `handleMessage` against closing a live overlay when same round_num) remains a TODO.
- **pg_data volume conflict:** Switching between dev and prod stacks requires `docker volume rm tensies_pg_data tensies_prom_data` each time. Consider documenting a `make switch-to-prod` / `make switch-to-dev` helper or using separate named volumes for each stack.
- **Step 27 first-rounds capture miss:** Install observer before `start-btn` click (not after) to catch R1/R2 overlays next run.
