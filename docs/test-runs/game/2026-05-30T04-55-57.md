# Test run — 2026-05-30T04:55:57  (closing the two remaining gaps)

Ran the two scenarios previously covered only by reasoning, now end-to-end and live.
**Telemetry excluded.** No product code changed — both required only test scaffolding.

## Results

```
 PASS  Pause cap fires -> fatal frame -> both clients land
 PASS  Real server restart during pause -> both clients land gracefully
```

## #1 — Pause cap (`PAUSE_MAX`) actually firing

- Launched the real app with `PAUSE_MAX` monkeypatched to **8 s** at boot
  (`/tmp/run_short_pause.py`: `import main; server.ws.PAUSE_MAX = server.broadcast.PAUSE_MAX = 8;
  uvicorn.run(main.app)`). Product code untouched.
- Browser (host + guest): started, host paused (countdown showed `0:08`, guest on the
  "Waiting for Host to resume the game" screen). Waited out the cap.
- At 8 s the server logged `pause_timeout game=GHOJM (paused > 8s)`; **both** clients
  received the fatal frame and landed on the landing screen with
  `#landing-error` = "Game ended — it was paused too long." and `localStorage` cleared.
- Exercises the full path: `pause_timeout()` → `game_ended` emit + `games_active.dec()` +
  cleanup → `{type:'error', fatal:true}` broadcast → client fatal handler → landing.

## #2 — Real in-process server restart during a pause

- Normal server (`PAUSE_MAX` = 1 h). Coordinated via a ready-file: the browser script
  set up a paused game, wrote `/tmp/restart_ready`, then waited for both clients to land;
  a separate shell command did the **actual** `kill -9` + relaunch of `uvicorn main:app`
  from the repo dir, wiping all in-memory game state.
- Both paused clients (host on the board, guest on the wait screen) detected the dropped
  socket, ran the reconnect loop against the down-then-up server, got "Game not found" on
  the rebuilt server, and landed via `expireSession()` — `#landing-error` = "Connection
  failed", sessions cleared. PASS.
- This is the genuine restart (vs the earlier deterministic stand-in in
  `2026-05-30T03-49-54.md`, which pointed the client at a missing game).

## Findings

None. Both behave exactly as designed.

## Notes

- The earlier "covered only by reasoning" gap is now closed: the cap end-of-game path and a
  true restart are both verified live, in addition to the existing server unit tests.
- **Harness friction (environment, not product):** in-sandbox `kill + relaunch` compound
  commands intermittently exit 1/144 and get terminated, and the local Postgres drops; the
  reliable pattern is to kill, confirm the port is free, then launch the server in a
  *standalone* command (and `pg_ctlcluster 16 main start` if needed). The real restart had
  to be driven from a separate shell command rather than in-process from Node (CWD issues).

## Watch next run
- Restoring a normal-`PAUSE_MAX` server after the short-cap test is mandatory before any
  other timing-sensitive test, or the 8 s cap will eject clients mid-test.
- Telemetry still untested by request (the `game_ended`/`games_active` effects of
  `pause_timeout` were observed via logs, not asserted against Postgres/Prometheus).
