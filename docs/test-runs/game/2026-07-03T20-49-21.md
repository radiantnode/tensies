# Test run — 2026-07-03T20:49:21

Branch: `feature/video-intro` (pre-merge confidence pass). Preflight: self-update **ran** (noted new `leave` action + `photo` field + `do_drop(grace_ms=0)` — additive; existing steps still cover the surface, skill not rewritten mid-run), prior-run review **ran** (2026-06-17: 31/31, WebAuthn RP_ID caveat). (Not scored.)

## Results

| # | Result | Description |
|---|--------|-------------|
| 01 | ✅ PASS | Server health |
| 02 | ✅ PASS | Landing screen (random placeholder) |
| 03 | ✅ PASS | Create game + lobby (solo hint; list excludes self — branch design) |
| 04 | ✅ PASS | Deep-link join + lobby sync; **gold HOST badge** validated from guest view |
| 05 | ✅ PASS | Invalid game code rejection ("Game not found") |
| 06 | ✅ PASS | Start + render, both clients synced (video intro → board clean) |
| 07 | ✅ PASS | Join-after-start rejection ("Game already in progress") |
| 08 | ✅ PASS | Roll + reveal (count 0→1, dice clean, no tearing) |
| 09 | ✅ PASS | Same-value re-roll — no hang (5 rolls all settled) |
| 10 | ✅ PASS | Rate limit logged + button recovered |
| 11 | ✅ PASS | Multiplayer broadcast timing (1761ms — in 800–2000 window) |
| 12 | ✅ PASS | Roll to win + winner overlay (Alpha/Winner/R1) |
| 13 | ✅ PASS | Sticky winner overlay regression (openWindow 3020ms, 30 spacebar fires, not stuck) |
| 14 | ✅ PASS | Round transition + target cycle (1→2→3) |
| 15 | ✅ PASS | Host pause toggle + non-host wait screen (board stays live) |
| 16 | ✅ PASS | Pause status panel + drops suspended + host returns to open menu |
| 17 | ✅ PASS | Resume returns everyone to play |
| 18 | ✅ PASS | Player disconnect mid-game (host → loading, new dice loader) |
| 19 | ✅ PASS | Player reconnect + token-gate negative check |
| 20 | ✅ PASS | Host disconnect + reconnect |
| 21 | ✅ PASS | Animation integrity (no tearing/clipping) |
| 22 | ✅ PASS | Winner/loser overlay consistency — 6 rounds, all 2997–3089ms, no flash, 1W+1L per round |
| 23 | ✅ PASS | Console clean (only test-induced WS closes + benign VT notices) |
| 24 | ✅ PASS | Virtual authenticator + signin screen |
| 25 | ✅ PASS | Sign-up (register passkey) — **real WebAuthn**, user in Postgres |
| 26 | ✅ PASS | Signed-in landing state |
| 27 | ✅ PASS | Signed-in gameplay (username as player name) |
| 28 | ✅ PASS | Sign-out |
| 29 | ✅ PASS | Sign-in (existing account) + both negative checks |
| 30 | ✅ PASS | Switch to prod build (esbuild pipeline incl. video fix ran clean) |
| 31 | ✅ PASS | Bundle structure (1 hashed JS, 2 CSS, gzip, immutable) + **video assets fingerprinted** |
| 32 | ✅ PASS | Browser resource count (13, bundled, no individual modules) |
| 33 | ✅ PASS | 3-round game on prod build (complementary W/L, 0 console errors) |
| 34 | ✅ PASS | Restore dev stack |

**34/34 passed.** WebAuthn ran the **real** ceremony (not degraded) via a `WEBAUTHN_RP_ID=localhost` override.

## Findings

- **None (no product failures).** The branch's frontend rework (video intro, dice loader on loading/pause, lobby animations + gold HOST badge, a2hs restyle) and the earlier lobby/menu commits all behave correctly end-to-end, including the pause/reconnect/host-transfer paths and the multi-round overlay-flash regression.
- **Build fix validated in prod:** the `scripts/build_assets.mjs` change (fingerprint `/static/video`, drop dev cache-bust queries) produces a correct prod bundle — `poster-landing-<hash>.webp` / `landing-hevc-<hash>.mp4` are hashed, served, gzip, immutable. This was the CI `frontend` failure; now green.

## Notes

- **Skill drift (not a bug):** Step 3's legacy assertion ("Alpha + HOST in `#lobby-players`") no longer holds — the reworked lobby excludes self and shows the "Invite friends or play solo!" hint for a solo host. The HOST badge is validated from the *guest's* view in Step 4 instead. Worth updating the skill text.
- **Local prod-smoketest env gaps (not product bugs):** `.env.prod` is missing `JWT_SECRET` and has empty passwords, so Steps 30–34 needed dummy secrets supplied via env. Reusing the shared postgres volume across dev/prod with different passwords caused `InvalidPasswordError` twice (prod up, and dev restore) — each fixed with `down -v` to re-init postgres. The dev DB is now fresh.
- Broadcast timing landed at 1761ms (upper end of the 800–2000 window but well clear of the <200ms regression threshold).

## Watch next run

- Update Step 3 assertions to the current lobby (self-excluded list + solo hint).
- Consider adding an explicit **`leave` action** step (lobby Back button — new on this branch; drops immediately, animates the row out) and a **lobby avatar (`photo` field)** check.
- The postgres volume/password dance in Steps 30–34: if `.env.prod` stays incomplete, expect to supply dummy secrets + `down -v`. A populated local `.env.prod` (or a dedicated smoketest env) would remove the friction.
