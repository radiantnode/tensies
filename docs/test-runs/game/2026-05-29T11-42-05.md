# Test run — 2026-05-29 (second run; validates skill updates)

## Results

TENSIES TEST RESULTS

Preflight: self-update none (no game-code changes), prior-run review ran (not scored)

| # | Result | Description |
|---|--------|-------------|
| 01 | ✅ PASS | Server health (OK; games_active=1 leftover from prior run, drained on re-nav) |
| 02 | ✅ PASS | Landing screen (placeholder "Frosty Pickle", #landing-error EMPTY — hygiene step worked) |
| 03 | ✅ PASS | Create game + lobby (code PPBEG, Alpha + HOST, solo msg) |
| 04 | ✅ PASS | Deep-link join + lobby sync (prefilled, URL cleaned, both synced) |
| 05 | ✅ PASS | Invalid game code rejection (ZZZZZ → "Game not found") |
| 06 | ✅ PASS | Game start + initial render (Round 1, target die, 0/10, both on #game) |
| 07 | ✅ PASS | Join-after-start rejection ("Game already in progress") |
| 08 | ✅ PASS | Roll + reveal (rc 0→1, btn re-enabled 1380ms) |
| 09 | ✅ PASS | Same-value re-roll (5 rolls 1072–1439ms, all advanced, no hang) |
| 10 | ✅ PASS | Rate limit + recovery (raw-WS method per updated step; server RATE LIMIT log; btn ok) |
| 11 | ✅ PASS | Multiplayer broadcast timing (1375ms — within band) |
| 12 | ✅ PASS | Roll to win + winner overlay ("Alpha", "Next up: roll for 5s") [open-attr capture] |
| 13 | ✅ PASS | Sticky winner overlay regression (29 fires, openWindow 3006ms, stillOpen false) |
| 14 | ✅ PASS | Round transition + target cycle (R1→R2→R3, target 6→5→4, clean reset) |
| 15 | ✅ PASS | Player disconnect mid-game (Tab1 loading, disconnected:true) |
| 16 | ✅ PASS | Player reconnect flow (reload + re-seed; identity 6ac6c1fb preserved; both back) |
| 17 | ✅ PASS | Host disconnect + reconnect (Beta loading, host unchanged; host reload restored) |
| 18 | ✅ PASS | Animation integrity (no tearing/clip; flags clear at rest) |
| 19 | ✅ PASS | Console clean (0 errors/warnings both instances) |

19/19 passed

## Findings

None.

## Notes

- **Clean run; skill updates validated.**
  - Step 2's new "clear localStorage + re-navigate" hygiene worked: the landing was clean on the first assertion (placeholder "Frosty Pickle", #landing-error empty) — no stale "Connection failed" this run (contrast prior run, which saw it before the fix).
  - Step 10's rewritten raw-WS rate-limit method worked exactly as documented: server logged `roll game=PPBEG player=Alpha RATE LIMIT`, button not stuck. The old double-click would have been a no-op.
- Broadcast timing 1375 ms — mid-band. Series healthy.
- Reconnect identity preserved across the reload (Beta 6ac6c1fb, Alpha 35864de1); isolated instances continue to make reconnect reliable.
- Throwaway edge-case tabs (Steps 5/7) again cleared instance #2's localStorage via their own doomed page-load reconnect; Step 16 re-seed handled it as designed. (Capturing each player's pid at join remains mandatory.)
- Target cycle verified live 6→5→4; has_rolled clean in the fresh round after the spacebar-hammer step (no orphan-tick contamination).

## Watch next run

- **Startup robustness:** at the very start of this run both Playwright pages had been closed since the prior session, so the first `browser_navigate` to each instance errored with "Target page/context closed"; a single retry relaunched them. If this recurs, add a note to Step 2 to expect a one-retry relaunch.
- Host-transfer after the 60s drop is still unverified (optional slow step).
- Persistent-profile localStorage hygiene (clear at start, re-seed before reconnect) remains essential — keep doing it.
