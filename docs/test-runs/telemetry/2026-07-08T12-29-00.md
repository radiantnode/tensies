# Telemetry test run — 2026-07-08T12:29:00

Branch: `claude/multiplayer-game-discovery-gps-ttcma6`. First telemetry pass since the discovery/lobby-stamp branch. Preflight: self-update **ran** (4 skill fixes — see Findings), prior-run review **ran** (2026-05-30: 17/18, the one WARN was the by-design Step 9 live-panel No-data, which did **not** recur this run).

## Results

| # | Result | Description |
|---|--------|-------------|
| 01 | ✅ PASS | Server + Prometheus health (after adding the METRICS_TOKEN bearer auth) |
| 02 | ✅ PASS | Grafana health + all 5 dashboards provisioned |
| 03 | ✅ PASS | Postgres health + 9 migrations, `events_2026_07` partition, 10 tables |
| 04 | ✅ PASS | Pre-game baseline (all Prometheus counters 0 — web fresh since today's restarts) |
| 05 | ✅ PASS | Played a game (ZJPAA, Telemetry/Monitor); mid-game Live panels showed real data |
| 06 | ✅ PASS | Event log complete — all 8 required types; 78 rolls, 0 missing payloads, max_matched=10 |
| 07 | ✅ PASS | Rollups: ended, peak_players=2, 3 rounds w/ winners, targets 1→2→3, roll sums reconcile (78) |
| 08 | ✅ PASS | Prometheus delta EXACT: rolls Δ78 = Postgres 78 (0%); games ±1; sessions Δ2; games_active→0 |
| 09 | ✅ PASS | live-games dashboard — 10 panels, 0 No-data, 0 errors (scoped window; prior WARN gone) |
| 10 | ✅ PASS | per-game dashboard — 12 panels; stats match Postgres (rounds=3, rolls=78, players=2) |
| 11 | ✅ PASS | connections dashboard — 10 panels, 0 errors |
| 12 | ✅ PASS | gameplay-health dashboard — 12 panels, 0 errors |
| 13 | ✅ PASS | analytics dashboard — 14 panels, 0 errors |
| 14 | ✅ PASS | Dice fairness — all faces 0.77–1.25× mean (sample 311, meaningful) |
| 15 | ✅ PASS | Timeline sanity — all 7 anomaly checks = 0 |
| 16 | ✅ PASS | Telemetry self-metrics — 0 drops, queue depths 0, 120 batches written |
| 17 | ✅ PASS | Grafana Live — push count 134, 0 failures, all 5 channels registered |
| 18 | ✅ PASS | Server log audit — no tracebacks, no unexpected errors (web + postgres) |

**18/18 passed.**

## Findings

- **Skill self-update (4 fixes this run):**
  1. **`/metrics` is now bearer-gated in dev** (`METRICS_TOKEN` in `.env`) — unauthenticated curls got `401`. Added `-H "Authorization: Bearer $METRICS_TOKEN"` guidance (Steps 1/4/8/16/17) and a `docker compose exec … printenv METRICS_TOKEN` grab.
  2. **Stale `#lobby-code` selector** (Step 5) → `_state.gameCode` (the discovery branch replaced the code box with `<lobby-stamp>`).
  3. **Prometheus scrape-target filter** looked for `:8888` but the app is scraped on its internal port `web:8000` — fixed the filter (target was healthy all along).
  4. **Per-game dashboard was redesigned** — Step 10 asserted gone/renamed panels (`Match progression (all rounds)`, `Dice distribution`, `Player wins`). Updated to the current set (`Luck balance (dice ahead of expectation)`, `Game event log`, `Rounds in this game`) + a note to take the fullPage screenshot before the text assertions (Grafana lazy-renders below-the-fold panels).
- No pipeline anomalies. The one prior WARN (Step 9 live-panel No-data) did **not** recur — scoping the dashboard to the game's exact `[started_ts, ended_ts]` window renders all panels.

## Pipeline measurements

- Game code: ZJPAA, players: Telemetry (host, 42 rolls, 3 wins) / Monitor (36 rolls, 0 wins)
- Rolls in test game: 78 (Alpha auto-roller counted 78 clicks; both players' rolls)
- Postgres roll events: 78 — matches Prometheus `rolls_total` delta **exactly (0%)**
- Round durations: R1 61.4s, R2 16.3s, R3 13.0s; targets 1→2→3
- Telemetry dropped (new this run): 0
- Live push failures (new this run): 0
- Queue depth at end: writer=0, live=0; batches written=120; live pushes=134
- Dice distribution (all-time view, n=311): [1:40, 2:51, 3:52, 4:43, 5:65, 6:60] — max ratio 1.25×

## Notes

- Prometheus counters baselined at 0 because the web process was restarted several times earlier today (dead-code sweep, `.env.test` round-trip) — in-process counters reset on restart; Postgres retained full history. Deltas were therefore clean and exact.
- New event types on this branch — `checked_in`, `player_left`, `reconnected`, `host_transferred`, `ack_timeout` — are emitted and landed in Postgres (this game produced `host_transferred`, `ack_timeout`, `player_left` from the host-navigates-away teardown). They are intentionally **not** asserted by this suite: `checked_in`/`player_left` need the GPS/check-in flow, which the plain 3-round game doesn't drive. A dedicated check-in telemetry test would be the place for `checked_in`.
- The host-navigates-away-first end sequence produced a `host_transferred` (Monitor briefly became host) before the disconnect-grace `game_ended` — expected, not an anomaly.

## Watch next run

- The `/metrics` bearer gate depends on `METRICS_TOKEN` being set in `.env`; if a future dev env leaves it unset the header is harmlessly ignored, but keep the token-grab in Step 1.
- If the per-game dashboard is re-laid-out again, Step 10's panel-name list will need another refresh — it's the most drift-prone step.
- Consider adding a `checked_in` telemetry assertion once there's a check-in-driving test harness (GPS mock), since that's this branch's headline new event.
