# Telemetry test run — 2026-05-29 19:08

## Results

TENSIES TELEMETRY TEST RESULTS

Preflight: self-update none (no code changes), prior-run review ran (1 prior log) (not scored)

| # | Result | Description |
|---|--------|-------------|
| 01 | ✅ PASS | Server + Prometheus health |
| 02 | ✅ PASS | Grafana health + provisioning |
| 03 | ✅ PASS | Postgres health + migrations |
| 04 | ✅ PASS | Pre-game baseline snapshot |
| 05 | ✅ PASS | Play a game (event generation) |
| 06 | ✅ PASS | Postgres event log completeness |
| 07 | ⚠️ WARN | Rollup table integrity — games.player_count=0 after clean end (all players dropped; field tracks current count not peak) |
| 08 | ✅ PASS | Prometheus delta cross-check |
| 09 | ⚠️ WARN | live-games dashboard — "Active games" + "Round progress per player" No data (live_games-backed, expected post-game; not yet in skill's allowed list) |
| 10 | ✅ PASS | per-game dashboard (post-game validation) |
| 11 | ✅ PASS | connections dashboard |
| 12 | ✅ PASS | gameplay-health dashboard |
| 13 | ✅ PASS | analytics dashboard |
| 14 | ✅ PASS | Dice fairness |
| 15 | ✅ PASS | Timeline sanity |
| 16 | ✅ PASS | Telemetry self-metrics |
| 17 | ✅ PASS | Grafana Live push health |
| 18 | ⚠️ WARN | Server log audit — 1 Postgres ERROR from test run's own malformed query (column alias in ORDER BY cast); not application error |

15/18 passed, 3 WARN, 0 FAIL

## Findings

- **WARN Step 7 — games.player_count=0 after clean game end.** When both players are dropped after DISCONNECT_GRACE, player_count decrements to 0 in the games rollup. Skill pass criterion said "= 2" but clean-end behaviour makes this 0. Update skill to accept player_count=0 when status=ended.

- **WARN Step 9 — live-games dashboard: 2 No-data panels post-game.** "Active games" and "Round progress per player" both query live_games which is cleared on game end. Same class of panel as the per-game dashboard Status/Round/Target panels. Skill needs to document these as expected No data post-game.

- **WARN Step 18 — Postgres ERROR from test query.** `column "face" does not exist` — caused by a multi-statement query run during this test that used a column alias in ORDER BY with a cast (`ORDER BY face::int`). Not an application error. Fix: use `ORDER BY 1` or full expression. The production v_dice_roll_distribution view is unaffected.

## Pipeline measurements

- Game played: code LYRHK, 2 players (Telemetry / Monitor), 3 rounds won (Telemetry 2, Monitor 1)
- Rolls in test game: 71
- Postgres roll events: 71 (matches Prometheus delta exactly: 0% delta) ✓
- games_started_total delta: 1 ✓
- games_ended_total delta: 1 ✓ (NEW — game_ended now fires correctly via about:blank navigation)
- tensies_games_active: returned to 0.0 after game ended ✓ (NEW — playing metric verified)
- sessions_started_total delta: 2 ✓
- Telemetry dropped events (new this run): 0
- Live push failures (new this run): 0
- Queue depth at end: writer=0, live=0
- Writer batches processed: 1535
- Grafana Live pushes: 1799 (avg ~18ms per push)
- Dice distribution (v_dice_roll_distribution, 737 total rolls): [1:117(0.95x), 2:136(1.11x), 3:125(1.02x), 4:118(0.96x), 5:115(0.94x), 6:126(1.03x)] — max ratio 1.11x, well within 2.0 threshold
- Round durations: R1=28.2s (19 rolls, target 6), R2=16.3s (23 rolls, target 5), R3=18.9s (29 rolls, target 4)
- round_player sums match rounds.total_rolls for all 3 rounds ✓

## Notes

- **game_ended now fires cleanly.** Navigating both browsers to about:blank tears down the JS context, preventing auto-reconnect. After DISCONNECT_GRACE (60s), both players drop, game ends with status=ended, round_count=4, games_ended_total increments. This is the first run to confirm the full game lifecycle.
- **host_transferred event appeared (1 count).** When Telemetry (host) navigated away first, the server transferred host to Monitor before the grace period expired. Expected behaviour.
- **player_left: 4 events** (expected 2). Likely emitted once on disconnect and once on drop_player after DISCONNECT_GRACE for each player.
- **Step 3 table count check is broken in skill.** `grep -c "|"` returns 0 for single-column psql output (no "|" in single-column format). All 10 tables confirmed present via direct query; skill needs to fix the count assertion.
- **Step 10 bottom panels (dice distribution, player wins) not detectable via innerText.** Barcharts render as SVG; table panels below the fold may be lazy-loaded by Grafana. Visual inspection via screenshot is the reliable check for these.

## Watch next run

- **Step 7 pass criterion** — update to accept player_count=0 when games.status=ended.
- **Step 9 expected No-data** — add "Active games" and "Round progress per player" to the live-games dashboard's allowed No-data list (alongside the per-game dashboard's live_games panels).
- **Step 10 panel detection** — the innerText approach misses SVG barcharts and lazy-loaded panels; consider scrolling to bottom or checking panel count by title instead.
- **Step 3 table count** — fix the grep -c "|" to use a proper count method.
- **Step 18 test query** — fix game-specific dice distribution query: change `GROUP BY face ORDER BY face::int` to `GROUP BY 1 ORDER BY 1`.
