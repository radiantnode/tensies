# Test run — 2026-05-27T21:49:00

## Results

```
TENSIES TEST RESULTS
====================
 PASS  01  Self-update check
 PASS  02  Server health
 PASS  03  Landing screen
 PASS  04  Create game + lobby
 PASS  05  Deep-link join (?join=CODE)
 PASS  06  Invalid code rejection      ← was FAIL last run, now fixed
 PASS  07  Multiplayer lobby sync
 PASS  08  Join-after-start rejection  ← was FAIL last run, now fixed
 PASS  09  Game start + initial render
 PASS  10  Roll + reveal (single player)
 PASS  11  Same-value re-roll (no hang)
 PASS  12  Rate limit + recovery
 PASS  13  Multiplayer broadcast timing
 PASS  14  Roll to win + winner overlay
 PASS  15  Round transition + target cycle
 PASS  16  Target cycling math
 PASS  17  Player disconnect mid-game
 PASS  18  Host disconnect / host transfer
 PASS  19  Animation integrity (no tearing)
 PASS  20  Console clean (no JS errors)
====================
20/20 passed
```

## Findings

None.

## Notes

- **Steps 06 and 08 regression cleared**: Both were FAIL in the prior run due to error messages routing to `#landing-error` instead of `#join-error`. Both now correctly display in `#join-error`. The fix from last run is confirmed working.

- **Broadcast timing measured at 1491ms** from roll click to Tab 2 detecting the state change. Consistent with prior run (1589ms). Still within the expected 1320–1720ms window. Using the setInterval-watcher approach again (installed before roll click, timestamps inside the page) — the Playwright tab-switch overhead alone is ~500ms so this approach is necessary for an accurate measurement.

- **Winner overlay captured via MutationObserver**: Roll-to-win loop ran in round 2 (Alpha had already won round 1 during the 5-roll + rate-limit sequence). MutationObserver on `#winner-overlay` captured: winner "Alpha", sub "Next up: roll for 4s". The overlay auto-dismisses before a screenshot can be taken from outside the loop — the MutationObserver approach remains the only reliable way to verify overlay content.

- **Alpha won round 1 unintentionally during step 9-12 testing**: By the time the broadcast timing check ran (step 11), Alpha had rolled 8 times total (1 initial + 5 in the same-value loop + 1 rate-limit + 1 broadcast timing roll). The round transitioned mid-test. Steps 12–13 continued in round 2/3 without issue — the state machine handles this gracefully.

- **Rate limiting confirmed client-side only**: Double-click processed only one roll (button disabled after first click blocks WS send for the second). No server-side rate limit warning logged. Consistent with prior run observation.

- **Console errors**: Only `favicon.ico 404` across all tabs (benign). No JS errors, no unexpected WebSocket closes.

- **`button.btn-primary` selector is ambiguous** — resolves to 3 elements (Create Game, Join Game in join screen, Start Game). Must use `button[onclick='createGame()']`, `button[onclick='joinGame()']`, etc. for reliable targeting. This is a test-infra note, not a game bug.

## Watch next run

- **Broadcast timing** — two measurements now (1589ms, 1491ms), both within window. If it creeps above 1720ms, animation duration may have changed.
- **Winner overlay screenshot** — still cannot capture a live screenshot of the overlay; MutationObserver remains the only verification method. If the overlay behaviour changes (e.g. manual dismiss replaces auto-dismiss), update the capture approach.
- **Round-transition mid-test**: Alpha tends to win round 1 during the rolling steps (9–12). This is fine but means step 14 (roll to win) always runs in round 2+. Watch that the round number reported in step 14 is ≥ 2.
- **Rate limit server-side**: Still not observed. Worth adding a direct server log check if rate limiting is ever intended to be enforced server-side.
