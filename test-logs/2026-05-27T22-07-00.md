# Test run — 2026-05-27T22:07:00

## Results

```
TENSIES TEST RESULTS
====================
 PASS  01  Self-update check
 PASS  02  Server health
 PASS  03  Landing screen
 PASS  04  Create game + lobby
 PASS  05  Deep-link join (?join=CODE)
 PASS  06  Invalid code rejection
 PASS  07  Multiplayer lobby sync
 PASS  08  Join-after-start rejection
 PASS  09  Game start + initial render
 PASS  10  Roll + reveal (single player)
 PASS  11  Same-value re-roll (no hang)
 PASS  12  Rate limit + recovery
 PASS  13  Multiplayer broadcast timing
 PASS  14  Roll to win + winner overlay
 PASS  15  Round transition + target cycle
 PASS  16  Target cycling math
 PASS  17  Player disconnect mid-game
 PASS  18  Host disconnect / host transfer
 PASS  19  Animation integrity (no tearing)
 PASS  20  Console clean (no JS errors)
====================
20/20 passed
```

## Findings

None.

## Notes

- **Broadcast timing measured at 1029ms** — noticeably lower than prior runs (1491ms, 1589ms). Still within the 1000–2000ms pass window, but the trend is worth watching. If it drops below 1000ms in a future run, the delayed-broadcast fix may have regressed.

- **No server-side rate limit log** — double-click fired both clicks, first was processed (roll_count incremented), second was silently dropped client-side (button was disabled). No WS error message from server. Rate limiting remains client-side only, consistent with all prior runs.

- **Winner overlay via MutationObserver** — captured winnerName="Alpha", winnerSub="Next up: roll for 5s" correctly (target was 6, next is 5). Overlay auto-dismissed before screenshot, as expected. MutationObserver remains the only reliable verification path.

- **Host transfer confirmed via `currentState.host`** — after closing host tab, NonHost's `host` field updated to `myId`. Game remained playable, roll_count incremented on first post-transfer roll.

- **Alpha won round 1 during steps 9–12** — by broadcast timing step, Alpha had rolled 8 times total. Roll-to-win loop ran in round 1 continuation (6/10 already matched at step 12 entry). Expected behaviour per prior run notes.

- **Console errors** — only `favicon.ico 404` on the original Tab 0. No JS errors, no unexpected WebSocket closes across any tab.

## Watch next run

- **Broadcast timing** — three measurements now: 1589ms, 1491ms, 1029ms. The drop to 1029ms is the lowest yet. If it dips below 1000ms, flag as potential regression in delayed-broadcast logic.
- **Winner overlay screenshot** — still cannot capture live; MutationObserver is authoritative. No change needed unless auto-dismiss timing changes.
- **Round-transition mid-test** — Alpha consistently wins round 1 during steps 9–12 due to accumulated rolls. Roll-to-win loop always enters at partial-match state (≥5 matched). This is fine but means step 14 entry state should always show `round_num ≥ 1` and `matched > 0`.
- **Rate limit server-side** — still not observed. Consider adding a direct server log tail check if server-side rate limiting is ever intended.
