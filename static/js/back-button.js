// @ts-check

/**
 * Chevron-only markup for the round "Back" button, shared across screens
 * (styled by `.btn-back` in landing.css). The label is visually hidden but kept
 * for assistive tech since the button is icon-only.
 */
export const BACK_BUTTON_HTML = `<svg class="back-chevron" viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2.25" stroke-linecap="round" stroke-linejoin="round"><path d="M15 18 9 12l6-6"/></svg><span class="sr-only">Back</span>`;
