// @ts-check

/**
 * Chevron + label markup for the round "Back" chip, shared by the join screen
 * and the changelog panel (styled by `.btn-back` in landing.css).
 */
export const BACK_BUTTON_HTML = `<svg class="back-chevron" viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2.25" stroke-linecap="round" stroke-linejoin="round"><path d="M15 18 9 12l6-6"/></svg><span>Back</span>`;
