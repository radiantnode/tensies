# Top-level runtime dependencies. Exact transitive pins live in
# requirements.lock (use that for reproducible/prod builds).
fastapi==0.138.0
uvicorn[standard]==0.49.0
asyncpg==0.31.0
prometheus-client==0.25.0
httpx==0.28.1
redis==8.0.0

# Transitive pins to close known CVEs in the resolved tree. fastapi 0.136.3
# requires only starlette>=0.46.0, so these upgrade cleanly:
#   starlette>=1.0.1  — CVE-2025-62727 (FileResponse Range O(n^2) DoS, reachable
#                       via /static) and CVE-2026-48710 ("BadHost" Host-header
#                       path poisoning). Fixed at 1.0.1; we pin the resolved 1.2.1.
#   h11>=0.16.0       — CVE-2025-43859 (chunked request smuggling).
starlette==1.3.1
h11==0.16.0

# WebAuthn passkey auth + JWT session tokens
webauthn==2.8.0
PyJWT==2.13.0

# BLS12-381 signature verification for drand beacon
blspy==2.0.3
