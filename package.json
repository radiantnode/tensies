{
  "name": "tensies-assets",
  "version": "1.0.0",
  "private": true,
  "description": "Build-time frontend asset pipeline (bundle + minify + fingerprint). Not a runtime dependency — only used to produce dist/ in the Docker build.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build_assets.mjs",
    "typecheck": "tsc -p jsconfig.json --noEmit"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "typescript": "7.0.2"
  }
}
