---
category: Controls
---

# TextInput

Text field: inset dark well, amber border, centered text, pink focus ring. `code` renders the large uppercase 5-letter game-code style.

```tsx
<TextInput placeholder="Your name" maxLength={20} />
<TextInput code placeholder="ABCDE" maxLength={5} />
```
